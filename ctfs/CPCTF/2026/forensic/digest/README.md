ramdos

The password is an 8-digit number, but the server has rate limits in place, so it shouldn't be possible to brute-force it!
I've also implemented digest authentication, so it should be safe to publish the pcap file, right?
