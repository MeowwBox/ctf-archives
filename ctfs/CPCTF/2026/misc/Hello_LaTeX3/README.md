Alph_Suima

LaTeX, a document creation software, had a next-generation project called LaTeX3.
Ultimately, LaTeX3 was split into three layers, each of which is being integrated into the current LaTeX (LaTeX2e).
Within this, the programming layer, expl3, has a very strict naming convention.
The following file is an example of code written in expl3, but one command name is missing (it's shown as ???).

Please complete the command corresponding to ???

For `flag`, please enter the command name corresponding to `???` directly. (In the format CPCTF{???})
