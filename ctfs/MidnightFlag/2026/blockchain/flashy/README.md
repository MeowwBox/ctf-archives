redbeans

A permanent remote broker is available on one TCP port.

Connect with netcat:

Menu flow:

1 starts a fresh private blockchain instance for you
the service prints a secret note, your player private key, and the deployed addresses
your JSON-RPC endpoint is on the same public port and looks like:
The secret note identifies your own instance. Keep it. You need it to:

recover the instance information later
reset your chain
ask for the flag with menu option 3
Each private instance lasts 20 minutes. The public broker itself stays online for the whole event.

Goal: make Set.isSolved() return true.

The remote broker runs permanently on one public port. Each secret note maps to one private chain and that chain is destroyed as soon as the player successfully claims the flag.
