Description
You are in the Qemu and the flag is outside. The rest is on you.

There can be some subtle differences in the memory layout depending on the used kernel. To aid in debugging such problems we provide access to the remote environment via ssh. On the machine gdb and debug symbols are installed. You can install more tools with sudo and apt.
